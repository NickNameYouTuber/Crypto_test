<resources>

    <!-- Base application theme -->
    <style name="Base.Theme.QryptApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Общие настройки -->
        <item name="android:statusBarColor">@color/colorPrimaryDarkLight</item>
        <item name="selectedDotColor">@color/selected_dot_color</item>
    </style>

    <!-- Светлая тема -->
    <style name="Theme.QryptApp.Light" parent="Base.Theme.QryptApp">
        <item name="backgroundColor">@color/background_color_light</item>
        <item name="textColor">@color/text_color_light</item>
        <item name="dotColor">@color/dot_color_light</item>
        <item name="android:navigationBarColor">@color/text_color_dark</item>
        <item name="buttonStyle">@style/Widget.App.Button.Light</item>
    </style>

    <!-- Темная тема -->
    <style name="Theme.QryptApp.Dark" parent="Base.Theme.QryptApp">
        <item name="backgroundColor">@color/background_color_dark</item>
        <item name="textColor">@color/text_color_dark</item>
        <item name="dotColor">@color/dot_color_dark</item>
        <item name="android:navigationBarColor">@color/text_color_light</item>
        <item name="buttonStyle">@style/Widget.App.Button.Dark</item>
    </style>

    <!-- Стили кнопок -->
    <style name="Widget.App.Button.Light" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/text_color_light</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="rippleColor">@color/button_ripple_color_light</item>
    </style>

    <style name="Widget.App.Button.Dark" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/text_color_dark</item>
        <item name="backgroundTint">@android:color/transparent</item>
        <item name="rippleColor">@color/button_ripple_color_dark</item>
    </style>

    <style name="BottomNavigationViewStyle" parent="Widget.Design.BottomNavigationView">
        <item name="itemBackground">@android:color/transparent</item>
    </style>

</resources>